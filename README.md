# Lexical Analysis Library

A library for lexical analysis.

## Installation

```bash
pip install .
```

## Usage

Check the `examples` folder for a demonstration. It contains two files:
- A grammar description in `.txt` format.
- `demo.py` – a script that uses the library and the grammar to tokenize a given input string.

See `demo.py` for a complete working example.